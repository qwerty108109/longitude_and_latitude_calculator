name: longitude_and_latitude_calculator
description: A starting point for Dart libraries or applications.
version: 1.0.0
repository: https://github.com/qwerty108109/longitude_and_latitude_calculator.git

environment:
  sdk: ^3.1.3

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
